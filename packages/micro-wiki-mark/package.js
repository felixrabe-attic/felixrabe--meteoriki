Package.describe({
  summary: "A minimal wiki markup language"
});

Package.on_use(function (api, where) {
  // api.use(['minimongo', 'mongo-livedata', 'templating'], 'client');

  // api.add_files(['errors.js', 'errors_list.html', 'errors_list.js'], 'client');
});
